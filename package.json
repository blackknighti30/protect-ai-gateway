{
  "name": "protect-ai-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Plugin-based gateway for integrating third-party cameras and AI detections with UniFi Protect.",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
